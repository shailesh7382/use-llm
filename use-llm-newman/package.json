{
  "name": "use-llm-newman",
  "version": "1.0.0",
  "private": true,
  "description": "Newman API tests for use-llm services",
  "scripts": {
    "test": "newman run postman/use-llm-api.collection.json -e postman/local.environment.json --reporters cli",
    "test:html": "newman run postman/use-llm-api.collection.json -e postman/local.environment.json --reporters cli,htmlextra --reporter-htmlextra-export reports/newman-report.html"
  },
  "devDependencies": {
    "newman": "^6.1.3",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}

